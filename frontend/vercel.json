{
  "buildCommand": "npm install @rolldown/binding-linux-x64-gnu --no-save --force > /dev/null 2>&1 && tsc -b && npx vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://nexushr-backend.onrender.com/api/:path*"
    },
    {
      "source": "/((?!assets/|favicon.ico|vite.svg|.*\\.png|.*\\.json).*)$",
      "destination": "/index.html"
    }
  ]
}

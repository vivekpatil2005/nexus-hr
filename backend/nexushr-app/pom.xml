<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.nexushr</groupId>
        <artifactId>nexushr-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>nexushr-app</artifactId>
    <name>NexusHR Application</name>
    <description>Main Spring Boot application assembling all modules</description>

    <dependencies>
        <!-- All modules -->
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-common</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-auth</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-employee</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-attendance</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-payroll</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-performance</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-ai</artifactId>
        </dependency>
        <dependency>
            <groupId>com.nexushr</groupId>
            <artifactId>nexushr-notification</artifactId>
        </dependency>

        <!-- Infrastructure -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Monitoring -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
